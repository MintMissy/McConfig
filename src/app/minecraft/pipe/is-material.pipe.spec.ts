import { IsMaterialPipe } from './is-material.pipe';

describe('IsMaterialPipe', () => {
  it('create an instance', () => {
    const pipe = new IsMaterialPipe();
    expect(pipe).toBeTruthy();
  });
});
