import { StrictTypeOfPipe } from './strict-type-of.pipe';

describe('StrictTypeOfPipe', () => {
  it('create an instance', () => {
    const pipe = new StrictTypeOfPipe();
    expect(pipe).toBeTruthy();
  });
});
