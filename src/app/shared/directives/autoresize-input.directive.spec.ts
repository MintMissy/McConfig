import { AutoResizeInputDirective } from './autoresize-input.directive';

describe('AutoresizeInputDirective', () => {
	it('should create an instance', () => {
		const directive = new AutoResizeInputDirective();
		expect(directive).toBeTruthy();
	});
});
