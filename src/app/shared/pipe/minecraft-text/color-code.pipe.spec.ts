import { ColorCodePipe } from './color-code.pipe';

describe('ColorCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorCodePipe();
    expect(pipe).toBeTruthy();
  });
});
