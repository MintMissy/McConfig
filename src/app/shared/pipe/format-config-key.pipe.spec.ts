import { FormatConfigKeyPipe } from './format-config-key.pipe';

describe('FormatConfigKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatConfigKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
